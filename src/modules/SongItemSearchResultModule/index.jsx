import { SongItemSearchResultComponent } from "src/components";

export const SongItemSearchResultModule = ({
    data
}) => {
    return (
        <SongItemSearchResultComponent data={data} />
    );
};