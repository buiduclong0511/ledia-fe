import styled from "styled-components";
import { Layout } from "src/layout";

export const App = () => {
    return (
        <Container>
            <Layout />
        </Container>
    );
};

const Container = styled.div``;