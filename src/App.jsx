import { Layout } from "src/layout"
import { StyledBackground } from "src/components";

export const App = () => {
    return (
        <div className="App">
            <StyledBackground />
            <Layout />
        </div>
    );
};